
export function polycurve ( env , polynomial ) {
	// if the curve equation is ax² + bx + cy² + dy + e then
	// polynomial must be of the form [ a , b , c , d , e ]
	// find zeros of polynomial in R[x,y]
	throw 'Not implemented' ;
}
